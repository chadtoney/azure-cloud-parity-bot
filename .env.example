# Azure OpenAI – update these values before running
# Get the endpoint from your Azure AI Foundry resource in the Azure Portal
AZURE_OPENAI_ENDPOINT=https://<your-resource-name>.cognitiveservices.azure.com/
# Leave blank to use Entra ID auth (az login) instead of an API key (recommended)
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Azure AI Foundry – enables agents visible in the Foundry portal (https://ai.azure.com)
# Format: https://<account-name>.services.ai.azure.com/api/projects/<project-name>
# Find this on the project Home page in the portal (New Foundry toggle ON)
# Leave blank to skip Foundry registration and use local Agent Framework only
AZURE_AI_FOUNDRY_PROJECT_ENDPOINT=https://<your-resource-name>.services.ai.azure.com/api/projects/<your-project-name>
AZURE_AI_FOUNDRY_AGENT_NAME=Azure Cloud Feature Parity Bot

# Agent settings
AGENT_MAX_ITERATIONS=10
AGENT_TEMPERATURE=0.0

# Storage paths
DATA_DIR=data/features
REPORTS_DIR=reports

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/parity-bot.log

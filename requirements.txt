# Microsoft Agent Framework — pin to avoid breaking renames
agent-framework-azure-ai==1.0.0b260107
agent-framework-core==1.0.0b260107

# Agent HTTP server (Agent Inspector & local dev)
azure-ai-agentserver-core==1.0.0b10
azure-ai-agentserver-agentframework==1.0.0b10

# Azure AI Foundry SDK (agents, projects, model deployment)
azure-ai-projects>=2.0.0b3
azure-ai-agents>=1.0.0

# Azure Identity (DefaultAzureCredential for Foundry auth)
azure-identity>=1.16.0

# HTTP / scraping
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Data models & config
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Environment & logging
python-dotenv>=1.0.0
loguru>=0.7.0

# Debugging (VS Code Python Debugger + Agent Inspector)
debugpy
agent-dev-cli --pre
